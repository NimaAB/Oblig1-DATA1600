public class readFile {
    public static ArrayList<String> readCountryCodes() throws IOException {
        BufferedReader in = new BufferedReader(new FileReader("countryCodes.txt"));
        String str;

        ArrayList<String> list = new ArrayList<>();
        while((str = in.readLine()) != null){
            list.add(str);
        }
        return list;
    }
}